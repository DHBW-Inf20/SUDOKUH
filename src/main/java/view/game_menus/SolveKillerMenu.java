package view.game_menus;

import java.awt.event.ActionListener;

public class SolveKillerMenu extends SolveMenu {
    public SolveKillerMenu(int size, ActionListener buttonListener, String title, String theme) {
        super(size, buttonListener, title, theme);
    }
}
